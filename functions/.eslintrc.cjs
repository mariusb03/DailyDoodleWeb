/* eslint-env node */
module.exports = {
  root: true,
  env: {
    es2021: true,
    node: true,
  },
  extends: ['eslint:recommended'],
  rules: {
    // you can relax rules later if needed
  },
};
